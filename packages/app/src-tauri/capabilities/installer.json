{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "installer",
  "description": "Window lifecycle access for the isolated installer UI",
  "windows": ["installer"],
  "permissions": ["core:default"]
}
